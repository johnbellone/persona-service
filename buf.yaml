build:
  roots:
    - proto
  excludes:
    - proto/google
lint:
  use:
    - BASIC
    - FILE_LOWER_SNAKE_CASE
  except:
    - PACKAGE_DIRECTORY_MATCH
    - PACKAGE_SAME_DIRECTORY
    - PACKAGE_AFFINITY
breaking:
  use:
    - WIRE_JSON
